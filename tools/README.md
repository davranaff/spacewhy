# Project tools

- obsidian-vault is an archived filesystem vault from the original scaffold. Resolve the active
  project vault through the project-memory bootstrap utility rather than attaching this repository
  to an unrelated vault.
- skills contains supplied reference packages. The Django guidance is retained as a historical
  reference only; the Spacewhy runtime contract is the FastAPI architecture under backend/docs.
