# Project tools

- `obsidian-vault/` is the project-scoped filesystem memory vault. It was bootstrapped from the supplied archive; Obsidian itself is not required or started.
- `skills/` contains the supplied `ruflo-django-backend` and `obsidian-project-memory` skill packages, including their references, scripts, and agent metadata.
