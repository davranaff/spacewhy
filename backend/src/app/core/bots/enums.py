"""Provider identifiers supported by the bot platform."""

from enum import StrEnum


class BotProvider(StrEnum):
    """Stable provider names accepted by typed settings."""

    TELEGRAM = "telegram"
