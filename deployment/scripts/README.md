# Operational scripts

Add reviewed, repeatable deployment, migration, reconciliation, backup, and rollback commands here. Every script must document its scope, required environment, dry-run behavior, and failure/rollback path before it is used against a shared environment.
