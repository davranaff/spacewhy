# Secret material boundary

This directory is intentionally empty. Real private keys, certificates, and provider credentials may be mounted here locally or supplied by a secret manager, but must never be committed, printed, or copied into `deployment/configs/`.
