# Deployment configurations

Put non-secret, environment-specific configuration here as the platform grows: reverse-proxy rules, observability defaults, resource policies, and migration/runbook metadata. Keep credentials and private material in the ignored secret boundary, never beside tracked config.
